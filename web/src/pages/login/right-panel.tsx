import styles from './index.less';

const LoginRightPanel = () => {
  return (
    <section className={styles.rightPanel}>
      {/* Empty panel - only login form will be visible */}
    </section>
  );
};

export default LoginRightPanel;
